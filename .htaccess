<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirige todo menos archivos y carpetas existentes a index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
</IfModule>